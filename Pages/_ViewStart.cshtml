@{
    Layout = "_Layout";
}

<matching-css-js verify-files="true"/>

@* 
    Optional Attributes are:
    verify-files: true/false
        True verifies that the files exist on the server before including them.
        False will include the files even if they don't exist, resulting in a 404's instead.
    css-only: true/false
        Loads only the css files
    js-only: true/false
        Loads only the js files
*@